package personal.cluster_management;

/**
 * Enum representing the UI states for the Monitor.
 * Moved to its own file to be used by the IMonitorUI interface.
 */
public enum MonitorPaneEnum {
    notConnected,
    gauges
}